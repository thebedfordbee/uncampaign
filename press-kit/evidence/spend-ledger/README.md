# evidence/spend-ledger/

Frozen, dated captures of the campaign's spending — the single most-cited fact in the kit, so it needs receipts.

## Capture list (from the outreach angle audit)

- [ ] **Spend ledger, frozen + dated** — a screenshot and a saved copy of `/footprint` showing the current total ($13.12 at time of writing) and the $49.99 cap.
- [ ] **Linked source records** — a saved copy/reference to the New York State Board of Elections records behind the 2021 comparison ($118,021), and the campaign's own itemization.

## Rules

- The Footprint Tracker (`/footprint`) is the source of truth. Snapshots here are dated copies of it, not a separate ledger.
- Re-capture whenever the spend total changes, so there's a timeline.
- Name files `YYYY-MM-DD-spend-<total>.png` (e.g. `2026-05-26-spend-13-12.png`).
